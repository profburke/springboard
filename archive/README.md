# Archive

This directory contains historical code, demos, screenshots, and packaging
experiments from the old `ios-icons` project.

Nothing here is active source. Do not copy APIs, examples, or dependency
patterns from this directory into new code without re-validating them against
the current `springboard` model.
